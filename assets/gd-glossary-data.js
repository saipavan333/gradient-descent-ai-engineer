window.GD_DEFS={"bias":"A systematic error from a model being too simple to capture the pattern (underfitting); also, the constant term added inside a neuron.","variance":"How much a model's predictions change with different training data — high variance means it's overfitting to noise.","parameters":"The numbers inside a model (weights and biases) that learning adjusts to fit the data.","hyperparameter":"A setting you choose before training (learning rate, layers, k) — not learned from data.","test set":"Held-out data used once at the end to estimate real-world performance; never used for training or tuning.","training set":"The data the model learns from by adjusting its parameters.","validation set":"Data held out from training to tune hyperparameters and catch overfitting, keeping the test set untouched.","cosine similarity":"How aligned two vectors are, ignoring length: their dot product divided by their magnitudes; 1 = identical direction.","pipeline":"An ordered chain of steps (ingest → features → train → serve) that data flows through.","convex":"A bowl-shaped loss with a single global minimum, so gradient descent can't get stuck in a bad local minimum.","softmax":"Turns a vector of scores into probabilities that sum to 1 by exponentiating and normalizing.","filter":"A small grid of weights (kernel) slid across an image in a CNN to detect one pattern everywhere.","kernel":"In CNNs, the small weight grid slid over the input; in SVMs, a function that measures similarity in a higher-dimensional space.","array":"A grid of numbers stored contiguously in memory; NumPy's core data structure for fast math.","query":"In attention, the vector representing what a token is looking for; compared against keys.","key":"In attention, the vector a token offers to be matched against queries.","value":"In attention, the information a token hands over, weighted by how well its key matched the query.","index":"In a vector database, the data structure that makes nearest-neighbour search fast.","backbone":"The pretrained feature-extracting body of a network, reused and topped with a task-specific head.","head":"The small task-specific layers on top of a backbone (e.g., a classifier head); in transformers, one of several parallel attention units.","low-rank":"Approximating a big matrix with the product of two thin ones — the idea behind LoRA's cheap fine-tuning.","monitoring":"Watching a deployed model's inputs and metrics over time to catch drift and failures.","feature":"An input variable the model uses to make predictions (e.g., a pixel, a word count, an age).","feature vector":"The list of numbers representing one example, fed into the model.","model":"A function with learnable parameters that maps inputs to predictions.","unsupervised learning":"Finding structure (clusters, dimensions) in data that has no labels.","supervised learning":"Learning a mapping X → y from labeled examples.","class imbalance":"When one class vastly outnumbers another, so accuracy misleads and metrics like recall matter more.","stratified":"Splitting data so each fold keeps the same class proportions as the whole.","dimensionality reduction":"Compressing many features into a few while keeping most of the information (e.g., PCA).","explained variance ratio":"The fraction of the data's total variance captured by each principal component.","scree plot":"A chart of explained variance per component, used to pick how many components to keep.","weight":"A learnable multiplier on an input inside a model; training tunes all the weights.","cross-entropy":"The standard classification loss: it punishes confident wrong predictions heavily.","bagging":"Training many models on random data subsets and averaging them to cut variance (e.g., random forests).","boosting":"Training models in sequence, each fixing the previous one's errors (e.g., gradient boosting).","vision transformer":"A transformer that treats image patches as tokens — 'an image is worth 16×16 words'.","bounding box":"The rectangle an object detector draws around each detected object.","forward process":"In diffusion, gradually adding noise to an image — the part that needs no learning.","reverse process":"In diffusion, gradually removing predicted noise to generate an image — the learned part.","defense in depth":"Layering multiple independent safeguards so no single failure is catastrophic.","gate":"A learned unit that controls how much information flows through (as in LSTMs/GRUs).","epoch":"One full pass of the training algorithm over the entire training dataset.","batch":"The group of examples processed together in one optimization step.","mini-batch":"A small batch (e.g., 32–256 examples) used per gradient step for speed and stability.","momentum":"An optimizer trick that accumulates a velocity of past gradients to speed up and smooth descent.","dropout":"Randomly zeroing units during training to prevent co-adaptation — a regularizer.","batch normalization":"Normalizing each layer's inputs per mini-batch to stabilise and speed up training.","residual connection":"A shortcut that adds a layer's input to its output, letting very deep networks train.","skip connection":"A shortcut passing earlier features forward (as in ResNets and U-Nets).","pooling":"Downsampling a feature map by taking the max or average of small regions.","stride":"How many pixels a filter jumps each step; larger stride shrinks the output more.","padding":"Adding border pixels so a convolution can keep the output the same size as the input.","receptive field":"The region of the input that influences one unit in a later layer.","logits":"The raw, unnormalized scores a model outputs before softmax turns them into probabilities.","sigmoid":"An S-shaped function squashing any number into (0,1) — used for probabilities and gates.","tanh":"An S-shaped activation squashing values into (−1,1), zero-centerd.","relu":"The activation max(0,x): keeps positives, zeros negatives — cheap and the deep-learning default.","gelu":"A smooth ReLU-like activation used in transformers.","perplexity":"A language model's uncertainty about the next token — lower is better.","cross-validation":"Rotating which fold is held out to estimate performance and tune without touching the test set.","k-fold":"Cross-validation that splits data into k parts, training on k−1 and validating on the last, k times.","grid search":"Trying every combination of a fixed set of hyperparameter values.","random search":"Sampling random hyperparameter combinations — often more efficient than grid search.","early stopping":"Halting training when validation performance stops improving, to prevent overfitting.","gradient clipping":"Capping gradient size to stop training from blowing up (exploding gradients).","vanishing gradient":"When gradients shrink toward zero through many layers, so early layers barely learn.","exploding gradient":"When gradients grow uncontrollably through layers, destabilising training.","one-hot":"Encoding a category as a vector that is 1 at its index and 0 elsewhere.","positional encoding":"Information added to inputs so the order-agnostic transformer knows token order.","multi-head attention":"Running several attention operations in parallel, each learning a different relationship.","self-attention":"Letting every position attend to every other via query-key-value, capturing long-range relationships directly.","cross-attention":"Attention where queries come from one sequence and keys/values from another (e.g., decoder attending to encoder).","encoder":"The half of a model that turns input into a rich representation.","decoder":"The half of a model that generates output, often one token at a time.","autoregressive":"Generating a sequence one token at a time, each conditioned on the previous ones (as in GPT).","causal mask":"Hiding future tokens so a model can only attend to the past — makes a left-to-right generator.","beam search":"Decoding that keeps the top-k partial sequences at each step to find a high-probability output.","greedy decoding":"Always taking the single most likely next token — fast but repetitive.","top-k":"Sampling only from the k most likely next tokens.","top-p":"Nucleus sampling: sampling from the smallest set of tokens whose probabilities sum to p.","temperature":"A decoding knob for randomness — low for factual/precise output, high for creative.","fine-tuning":"Further training a model on task-specific data to shape its behavior or style.","zero-shot":"Getting a model to do a task with no examples, just an instruction.","few-shot":"Prompting a model with a handful of examples to steer it at inference time.","in-context learning":"A model adapting to a task purely from examples in the prompt, without weight updates.","chain-of-thought":"Prompting the model to reason step by step, which improves performance on hard problems.","prompt":"The input text that steers a language model's output.","system prompt":"A hidden instruction that sets an assistant's role and rules for the whole conversation.","rlhf":"Reinforcement learning from human feedback — aligning a model with human preferences for helpfulness and safety.","reward model":"A model trained to score outputs by human preference, used to fine-tune an LLM.","quantization":"Running a model at lower numerical precision to cut memory and speed up inference, with little accuracy loss.","distillation":"Training a small 'student' model to mimic a big 'teacher' model.","pruning":"Removing unimportant weights to shrink and speed up a model.","lora":"Low-Rank Adaptation — fine-tuning by training a tiny low-rank adapter while freezing the model; cheap and swappable.","adapter":"A small trainable module inserted into a frozen model for cheap task adaptation.","peft":"Parameter-efficient fine-tuning — adapting a model by training only a small fraction of parameters.","reranking":"A second, precise pass (often a cross-encoder) that reorders retrieved candidates for quality.","chunking":"Splitting documents into passages before embedding; you can only retrieve what your chunks contain.","vector database":"A store of embeddings with fast approximate nearest-neighbor search, powering retrieval.","ann":"Approximate nearest-neighbour search — finding close vectors fast without checking every one.","recall@k":"Whether the relevant chunk is among the top-k retrieved — the key retrieval metric for RAG.","logistic regression":"A linear model for classification that outputs a probability via the sigmoid.","linear regression":"Fitting a straight line (or hyperplane) to predict a continuous value.","decision boundary":"The surface where a classifier switches from predicting one class to another.","activation":"The nonlinear function applied to a neuron's weighted sum, letting networks model complex patterns.","latency":"The time to serve a single request.","throughput":"How many requests a system handles per unit time.","autoscaling":"Automatically adjusting the number of serving replicas with traffic — enough at peak, no waste off-peak.","canary":"Releasing a change to a small slice of traffic first to catch problems early.","idempotent":"An operation that has the same effect whether run once or many times.","schema":"The defined structure and types a piece of data or a tool's arguments must follow.","embedding":"A dense vector representing a token or item, where similar things are close together.","tokenization":"Splitting text into the units (tokens) a model reads.","regularization":"Penalizing complexity (L1/L2, dropout, early stopping) to reduce overfitting.","generalization":"How well a model performs on new, unseen data.","vector":"An ordered list of numbers; the basic way data and parameters are represented.","matrix":"A grid of numbers; applying one is a linear transformation, and a matrix multiply is the core operation of ML.","gradient":"The vector of partial derivatives — which way, and how steeply, a function increases; points the way to adjust parameters.","gradient descent":"The learning algorithm: repeatedly step parameters opposite the gradient to reduce error.","learning rate":"The size of each gradient-descent step; too big diverges, too small crawls.","probability distribution":"A description of how likely each outcome is; models output and sample from these.","bayes' rule":"How to update a belief given new evidence — the backbone of many ML methods.","maximum likelihood":"Choosing parameters that make the observed data most probable; justifies common loss functions.","vectorization":"Replacing Python loops with array operations (NumPy) so numerical code runs fast.","data leakage":"When information from the test set (or the future) sneaks into training, inflating scores and ruining real performance.","overfitting":"Fitting the training data so closely (including its noise) that the model fails on new data.","bias–variance tradeoff":"Error splits into bias (too-simple/underfit) and variance (too-sensitive/overfit); complexity trades one for the other.","precision":"Of the items predicted positive, the fraction that truly were: TP/(TP+FP).","recall":"Of the actual positives, the fraction caught: TP/(TP+FN).","f1 score":"The harmonic mean of precision and recall — a single balanced metric.","confusion matrix":"The table of true/false positives and negatives from which precision and recall are computed.","ensemble":"Combining many models (e.g., a random forest) so their independent errors cancel — lowering variance.","neuron":"A weighted sum of inputs plus a bias, passed through a nonlinear activation.","activation function":"The nonlinearity (ReLU, sigmoid, tanh, GELU) that lets stacked layers model complex patterns.","backpropagation":"Computing every weight's gradient efficiently via the chain rule, in one backward pass.","vanishing gradients":"When gradients shrink toward zero through many layers, so early layers barely learn; fixed by ReLU, good init, normalization, residual connections.","cnn":"Convolutional neural network — uses shared local filters, ideal for images (locality + translation invariance).","rnn":"Recurrent neural network — processes sequences with a hidden state; largely superseded by transformers.","transfer learning":"Reusing a pretrained model and adapting only a small part to a new task — strong results from little data.","token":"The unit of text a model processes (a word or word-piece); text is split into tokens before modeling.","transformer":"The architecture built on stacked self-attention; the foundation of modern LLMs, prized for long-range modeling and parallelism.","context window":"The maximum number of tokens a model can attend to at once — a hard limit on input and history.","pretraining":"Learning language by predicting the next token on massive text — where an LLM gains its capability.","prompting":"Steering a fixed model at inference time via the input (few-shot examples, chain-of-thought).","hallucination":"When a model confidently generates false information, because it optimizes plausibility, not truth.","rag":"Retrieving relevant documents at query time and grounding the LLM's answer in them — for current, private, citable knowledge.","hybrid search":"Combining semantic (embedding) and keyword (BM25) retrieval so exact terms and meaning are both covered.","faithfulness":"Whether a generated answer is actually grounded in the retrieved context (vs hallucinated).","train/serve skew":"Features computed differently at training vs serving time — a silent accuracy killer.","agent":"An LLM in a loop that reasons, calls tools, observes results, and repeats to accomplish multi-step tasks.","tool use / function calling":"Letting a model emit a structured request that your code executes — search, code, an API.","react":"Interleaving Thought → Action → Observation so tool use is deliberate and the trace is debuggable.","guardrails":"Independent checks around a model (permission allowlists, budgets, human approval) that enforce safety regardless of the model.","prompt injection":"Malicious instructions hidden in content a model reads, attempting to hijack it — an unsolved security problem.","least privilege":"Giving a system only the minimum access it needs, so a compromised agent can't cause serious harm.","mlops":"The engineering of deploying, serving, monitoring, and maintaining ML in production — the lifecycle loop.","feature store":"A system that computes and serves features consistently for both training and serving, preventing skew.","model registry":"A versioned catalog of trained models with staged promotion and instant rollback.","serving":"Running a packaged model so it answers prediction requests — online (real-time), batch, or streaming.","latency vs throughput":"Time for one request vs total requests handled — online serving optimizes the first, batch the second.","drift":"When live data moves away from the training distribution, silently degrading a deployed model; fixed by retraining.","two-stage funnel":"Cheap wide retrieval then expensive narrow ranking — how recommenders and search scale.","qps":"Queries per second — the request rate a system must handle, sized for the peak, not the average.","caching":"Reusing stored results for repeated requests; savings scale with the hit rate.","goodhart's law":"When a measure becomes the target, it stops being a good measure — the trap of optimizing a single proxy.","offline vs online evaluation":"Testing on a fixed dataset (fast proxy) vs A/B testing on real users (ground truth).","a/b test":"A randomized experiment comparing a new system against the current one on live traffic.","llm-as-judge":"Using a capable model to grade outputs at scale — validated against human labels first.","disparate impact":"When a model's outcomes disproportionately harm a group; the four-fifths rule is a common red-flag check.","red-teaming":"Deliberately attacking your own system to find failures and jailbreaks before real adversaries do.","pii":"Personally Identifiable Information — data that must be minimized, protected, and often redacted.","reproducibility":"Getting the same result again by pinning code, data, environment, and the random seed.","image tensor":"An image as a height x width x channels grid of pixel numbers — all a model ever sees.","convolution":"Sliding a small learned filter over an image to detect a pattern anywhere it appears (weight sharing).","feature map":"The output of a convolution — where in the image a given filter's pattern was found.","data augmentation":"Random flips, crops, rotations, and color shifts that turn one image into many, improving generalization.","object detection":"Localizing and classifying each object — a bounding box plus a class label per object.","iou":"Intersection over Union — overlap area / union area of two boxes; a detection usually counts as correct at IoU >= 0.5.","segmentation":"Assigning a class to every pixel — the finest-grained vision task, giving exact shapes (semantic vs instance).","u-net":"An encoder-decoder with skip connections: compress to understand, expand to localize; used for masks and diffusion denoising.","vision transformer (vit)":"A transformer that treats image patches as tokens — 'an image is worth 16x16 words'.","inductive bias":"Assumptions built into an architecture; CNNs bake in locality/translation-invariance, ViTs assume less and need more data.","diffusion model":"A generator that learns to reverse noising: denoise from pure noise into a new image (Stable Diffusion, DALL-E).","latent diffusion":"Running diffusion in a compressed latent space (not raw pixels) to make text-to-image fast.","clip":"A model that aligns images and text in one shared embedding space via contrastive learning — enabling zero-shot classification.","multimodal / vlm":"A model working across data types (images + text); modern vision-language models feed image tokens into an LLM.","attention":"A mechanism where each token compares a query to every key and pulls a weighted mix of values — sharing information across a sequence.","chain rule":"The calculus rule for differentiating nested functions; multiply the derivatives along the path — the engine of backprop.","computational graph":"A diagram of operations as nodes; backprop flows gradients backward through it.","derivative":"How fast a function changes as its input changes — its slope at a point.","distribution":"A description of how likely each outcome is for a random quantity.","dot product":"Multiply two vectors element-wise and sum — measures how aligned they are.","element-wise":"Applying an operation to each corresponding pair of entries independently.","forward pass":"Running inputs through a network to produce an output (and the loss).","backward pass":"Propagating gradients from the loss back through the network to every parameter.","latent":"A hidden, learned representation — a compressed space of meaning (as in latent diffusion).","layer":"A group of neurons applied together; networks stack many layers.","layer normalization":"Normalizing a layer's activations per example — standard in transformers.","linear combination":"A weighted sum of vectors — the core operation inside every neuron.","mean":"The average of a set of numbers.","standard deviation":"How spread out numbers are around their mean.","supervised fine-tuning":"Fine-tuning a model on labeled instruction–response pairs to teach it to follow instructions.","scaling":"Rescaling features to comparable ranges; also, growing model/data/compute to improve performance.","scaling laws":"Empirical curves showing loss falls predictably as compute, data and parameters grow.","accuracy paradox":"When high accuracy hides poor performance because one class dominates.","action":"In an agent's loop, the tool call it decides to make.","agent loop":"The repeat cycle of think → act (call a tool) → observe until the agent can answer.","approximate nearest neighbor":"Finding close vectors fast without exhaustively checking every one.","attention matrix":"The grid of weights showing how much each token attends to each other token.","autoencoder":"A network trained to compress input to a code and reconstruct it — learns useful representations.","autograd":"Automatic differentiation — the framework feature that computes gradients for you.","automatic differentiation":"Computing exact derivatives of code by applying the chain rule mechanically.","axis":"A dimension of an array (rows vs columns, etc.) that operations can run along.","axis-aligned":"Splits or boundaries parallel to the feature axes, as a decision tree makes.","back-of-the-envelope estimation":"Rough order-of-magnitude math to size a system quickly.","batch serving":"Running predictions on large batches offline rather than one request at a time.","batching":"Grouping requests or examples to process together for efficiency.","bayesian optimization":"Smartly searching hyperparameters by modeling which settings are promising.","bert":"An encoder-only transformer that reads text bidirectionally — strong for understanding tasks.","bi-encoder":"Encoding query and document separately into vectors for fast retrieval.","cross-encoder":"Scoring a query–document pair jointly for accuracy — used in reranking.","bidirectional":"Reading a sequence in both directions at once (as BERT does).","bm25":"A classic keyword-ranking function for retrieval based on term frequency.","boolean mask":"An array of True/False used to select or filter entries.","broadcasting":"NumPy stretching size-1 dimensions so arrays of different shapes can combine.","byte-pair encoding":"A tokenizer that builds a vocabulary by repeatedly merging the most frequent adjacent pair.","candidate generation":"The cheap first stage that retrieves a wide set of items to later rank.","capacity":"How complex a pattern a model can represent — too little underfits, too much overfits.","causal":"Only allowed to depend on the past (earlier tokens), not the future.","cell state":"The long-term memory carried through an LSTM.","central limit theorem":"Averages of many independent samples tend toward a normal distribution.","centroid":"The mean position of a cluster's points — what k-means moves each step.","channels":"The stacked feature maps of an image or conv layer (e.g., RGB has 3).","chinchilla":"A finding that many LLMs were undertrained — compute is best split more toward data.","chunk size":"How large each retrieved passage is when splitting documents for RAG.","ci/cd":"Continuous integration/deployment — automated testing and release pipelines.","class label":"The category assigned to an example in classification.","classification":"Predicting which discrete category an input belongs to.","clustering":"Grouping unlabeled data by similarity.","commutative":"An operation whose order doesn't matter (a+b = b+a); matrix multiply is NOT.","compute":"The amount of processing (FLOPs/GPU-time) used to train or run a model.","concept drift":"When the relationship between inputs and the target itself changes over time.","condition number":"How ill-conditioned a problem is — high means gradients zig-zag and training is hard.","container":"A packaged, isolated environment (e.g., Docker) so code runs the same everywhere.","contrastive learning":"Training representations by pulling similar pairs together and pushing dissimilar ones apart.","convolutional neural network":"A network that slides shared filters over an image to detect patterns anywhere.","correlation":"How strongly two variables move together, from −1 to 1.","covariance matrix":"A table of how features vary together; PCA uses its eigenvectors.","curse of dimensionality":"In high dimensions, data becomes sparse and distances lose meaning.","data drift":"When live input data moves away from the training distribution.","data pipeline":"The automated flow that ingests, cleans and transforms data for models.","dataframe":"pandas' table structure of labeled rows and columns.","decision tree":"A model that splits data with a sequence of yes/no questions.","decoder-only":"A transformer that only generates left-to-right (the GPT family).","decoding":"Turning a model's next-token probabilities into actual generated text.","demographic parity":"A fairness criterion: outcomes are independent of a protected attribute.","deployment":"Putting a trained model into production to serve real requests.","determinant":"A number summarizing how a matrix scales area/volume; zero means it's non-invertible.","discriminator":"In a GAN, the network that learns to tell real data from generated fakes.","generator":"In a GAN, the network that learns to produce realistic fake data.","eigenvalue":"How much an eigenvector is stretched by a matrix.","eigenvector":"A direction a matrix only stretches, without rotating.","gan":"Generative Adversarial Network — a generator and discriminator trained against each other.","hidden state":"The evolving memory a recurrent network carries across a sequence.","hidden layer":"A layer between input and output where intermediate features are learned.","inference":"Running a trained model to make predictions (as opposed to training).","instance":"A single data example (row); also, one detected object in instance segmentation.","jailbreak":"A prompt that tricks a model into bypassing its safety rules.","k-means":"A clustering algorithm that alternates assigning points to nearest centroids and moving centroids.","knn":"k-nearest neighbours — classify a point by a vote of its k closest training points.","loss function":"The number a model tries to minimize, measuring how wrong its predictions are.","loss surface":"The landscape of loss over parameter values that optimization descends.","matrix multiplication":"Combining two matrices by dot-producting rows with columns — the core ML operation.","norm":"The length or magnitude of a vector.","normal distribution":"The bell curve — the most common distribution in statistics.","objective":"The quantity being optimized (usually minimizing a loss).","optimizer":"The algorithm that updates parameters from gradients (SGD, Adam, …).","underfitting":"A model too simple to capture the pattern, doing poorly even on training data.","pca":"Principal Component Analysis — projecting data onto the directions of greatest variance.","projection":"The shadow of one vector onto another's direction.","random forest":"An ensemble of decision trees on random data/feature subsets, averaged to cut variance.","recurrent":"A network that processes a sequence step by step, carrying a hidden state.","replica":"One running copy of a served model; more replicas add capacity.","reward":"The signal an agent or RLHF process optimizes for.","row":"One example (record) in a table; a horizontal slice of a matrix.","column":"One feature in a table; a vertical slice of a matrix.","span":"A contiguous slice of a sequence (e.g., an answer span in a document).","stochastic":"Involving randomness — as in stochastic gradient descent using random mini-batches.","sgd":"Stochastic gradient descent — updating weights from the gradient of a random mini-batch.","tensor":"A multi-dimensional array — the basic data object in deep learning.","transpose":"Flipping a matrix over its diagonal, swapping rows and columns.","variance (statistics)":"The average squared distance of values from their mean.","cross-entropy loss":"The standard classification loss, penalizing confident wrong predictions.","reranker":"A precise second-stage model that reorders retrieved candidates.","logit":"A raw pre-softmax score output by a model.","pooling layer":"A layer that downsamples feature maps, keeping the strongest signal per region."};